<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chinasoft.mapper.userMapper">
  
  <insert id="addUser">
  		insert into  user(username,password) values('sichuan','123456');
  </insert>
  
  <delete id="delUser">
   		delete from  user where id = 7;
  </delete>
  <update id="updUser">
  		update user set address='川大' where id = 1;
  </update>
  <select id="selectUser" resultType="com.chinasoft.pojo.User">
  	select 
  	id,username,password,phone,address,email from user where id = 3;
  </select>
</mapper>